package com.suonk.notepad_plus.ui.note.list

import com.suonk.notepad_plus.utils.EquatableCallback

data class NotesListActionViewState(
    val stringRes: Int,
    val onClicked: EquatableCallback,
)
